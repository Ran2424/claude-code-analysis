# Claude Code 技术框架总结

## 1. 它本质上是什么

Claude Code 不是“给终端套一层 API”的小工具，而是一套完整的交互式应用。它把模型调用、工具执行、权限控制、终端渲染、远程连接、扩展机制和长期记忆拼成了一个连续系统。用户表面上看到的是一个 CLI，对内部来说，它更像一个以终端为外壳、以 agent loop 为核心的应用平台。

如果只抓主线，可以把它理解成五层：

1. 入口与启动层：解析命令行参数、选择运行模式、加载配置、进入交互式或非交互式流程。
2. 查询执行层：把一轮用户输入变成“构造上下文 → 调模型 → 收流式输出 → 触发工具 → 回写结果 → 决定是否继续”的循环。
3. 工具与命令层：把外部能力和产品能力暴露给模型与用户。
4. UI 与状态层：在终端里维护一个 React 应用，让消息、输入、弹窗、权限请求、进度树都能稳定渲染。
5. 扩展与连接层：把 IDE、远程会话、MCP、skills、plugins、voice 等外围系统接进来。

也就是说，Claude Code 的核心不是某一个文件，而是“主循环 + 工具系统 + 终端 UI + 权限边界”这四块如何互相咬合。

---

## 2. 它是怎么启动起来的

启动链路从 `entrypoints/cli.tsx` 开始。这个文件先做一层很轻的分流：如果用户只是查版本、启 bridge、跑 daemon、进入某个特殊模式，它直接走对应的快速路径，不必把整个 CLI 都加载起来。只有普通使用场景，才会继续进入 `main.tsx`。

`main.tsx` 才是真正的总装入口。这里会做几件关键的事：

- 处理环境和安全设置。
- 解析 CLI 参数和斜杠命令相关配置。
- 初始化 settings、feature flags、telemetry、MCP、工具注册表等全局能力。
- 判断当前是交互式 REPL、打印模式、无头模式，还是远程 / bridge 模式。
- 最后把控制权交给查询系统和交互式 UI。

这一步很重要，因为 Claude Code 不是单一路径程序。它同时支持本地交互、无头调用、远程控制、后台任务、bridge 接入、MCP server 等多种运行形态，所以入口层必须先把模式判断清楚，再把后面的系统按需装起来。

---

## 3. 它真正的核心：查询循环

Claude Code 真正的“心脏”在 `query.ts` 和 `QueryEngine.ts`。

这里做的事情可以概括成一轮 agent 执行：

1. 收到用户输入。
2. 组合 system prompt、项目上下文、历史消息、记忆、工具定义和当前设置。
3. 调用 Claude API，流式接收模型输出。
4. 如果模型请求工具，就走权限判断，再执行工具。
5. 把工具结果重新塞回上下文，继续下一步推理。
6. 当模型给出最终答复，或者达到停止条件，这一轮结束。

这套循环不是简单的“问一次、答一次”。它更像一个受控代理：

- 模型能决定何时调用工具。
- 工具结果会反过来影响后续推理。
- 系统会追踪 token 预算、cost、历史和中断状态。
- 上下文过大时，会自动 compact，把旧内容压缩成更短的摘要。

所以 Claude Code 的产品能力，本质上来自这条循环把“语言模型”和“可执行环境”接到了一起。

---

## 4. 命令和工具为什么要分成两套

Claude Code 同时有 slash commands 和 tools，两者职责不同。

`commands` 面向用户。它们是产品层入口，比如 `/compact`、`/mcp`、`/plan`、`/review`。命令更像“用户显式触发的功能菜单”。

`tools` 面向模型。它们是模型能调用的原子能力，比如读文件、改文件、执行 shell、搜索、发起子 agent、访问 Web、调用 MCP。工具更像“模型可支配的能力接口”。

这两层分开很关键，因为它解决了两个问题：

- 用户需要可理解、可发现、可记忆的产品入口。
- 模型需要稳定、结构化、可校验、可授权的执行接口。

也因此，Claude Code 的能力不是写死在 prompt 里，而是通过一套正式的工具框架暴露出来。每个工具都有 schema、权限检查、只读 / 并发 / 破坏性标记、结果映射和 UI 展示方式。这样模型调用的不是“魔法”，而是一个受约束的执行系统。

---

## 5. 权限系统为什么是它的骨架

Claude Code 之所以能安全地把模型接到文件系统、shell 和网络，不是因为模型“足够聪明”，而是因为外面有一层独立的权限系统。

这套权限系统会在工具执行前决定：

- 这个工具能不能调用。
- 当前上下文里是否需要用户确认。
- 是否能自动放行。
- 这个请求属于只读、写入、执行还是高风险操作。

权限判断并不只是一个弹窗，而是一整套机制：配置规则、工具元数据、运行时上下文、UI 交互、sub-agent 限制、remote/bridge 特殊路径，全都要参与。也正因为这套骨架存在，Claude Code 才能把 agent 化能力推得很深，而不至于变成完全失控的执行器。

换句话说，Claude Code 不是“模型直接操作电脑”，而是“模型在受控权限框架内申请和使用能力”。

---

## 6. 终端 UI 为什么这么重

很多 CLI 只是输出字符串，但 Claude Code 不是。它把终端当成一个真正的应用界面来做。

UI 这一层由 React 组件、hooks、context 和自定义 Ink 框架组成。它需要处理的不是单纯文本，而是：

- 多轮消息流式刷新
- 输入框与 Vim 模式
- 权限对话框
- Agent 进度树
- 弹窗、列表、搜索、高亮
- 滚动、选择、焦点、快捷键
- 远程状态、后台任务、通知

官方 Ink 不够用，所以仓库里维护了一套深度改造过的终端渲染框架。内部有 React reconciler、虚拟 DOM、Yoga 布局、screen buffer、diff engine、ANSI tokenizer、终端事件系统。也就是说，Claude Code 的“终端界面”并不是普通 stdout，而是一套真正的渲染引擎。

这就是为什么它能在终端里呈现出接近 GUI 应用的交互复杂度。

---

## 7. 状态、服务和 hooks 是怎么托住这套 UI 的

如果说查询循环是心脏，`services + state + hooks` 就是血管系统。

`services/` 负责业务能力，比如 API client、analytics、compact、voice、memory、autoDream、rate limit、session 相关逻辑。它们尽量把“业务规则”从界面代码里抽出来。

`bootstrap/state.ts` 和相关 context 负责跨模块共享状态，比如 session id、模式、远程信息、活跃 agent、权限请求等。

`hooks/` 则把这些状态和行为接到组件里，形成 UI 可消费的接口。因为组件很多、交互很多，hook 数量也很多。它们不是锦上添花，而是把输入、通知、队列、快捷键、IDE、voice、tasks、remote 等各个子系统织在一起的连接层。

所以 Claude Code 的前端部分虽然跑在终端里，但组织方式和大型 React 应用并没有本质区别。

---

## 8. 它为什么能接 IDE、远程会话和 Web

Claude Code 并不把自己锁在本地终端里。它有一整套 bridge / remote 架构，把本地 CLI、远程会话、Web 界面、IDE 集成连起来。

这块大致做了三件事：

- 用 bridge 协议把本地状态和远程端同步。
- 用 SSE、WebSocket、Hybrid transport 在不同环境传输事件。
- 用认证、session、trusted device、remote permission bridge 保证跨端操作可控。

所以它既能在本地终端跑，也能做远程 viewer、远程控制、IDE handoff、云端会话同步。这部分不是边缘功能，而是 Claude Code 走向多端一致体验的关键。

从架构上看，Claude Code 已经不是一个“单机 CLI”，而是一个本地前端加远程控制面的混合系统。

---

## 9. 它如何扩展自己

Claude Code 的扩展能力主要来自四块：

- MCP：把外部能力以协议方式接进来。
- Skills：把高层工作流和领域知识接进来。
- Plugins：把本地功能、市场分发和可安装扩展接进来。
- Agents：把任务拆给其他 agent，形成多 agent 协作。

这四块层级不同：

- MCP 更偏“能力接入”。
- Skills 更偏“工作方法和高层指令”。
- Plugins 更偏“产品扩展包”。
- Agents 更偏“执行组织方式”。

它们叠在一起之后，Claude Code 就不只是一个固定功能集，而是一个可持续扩展的平台。

---

## 10. Memory、Buddy、Voice 这些“特殊系统”说明了什么

这些系统表面上看很杂，但其实说明了 Claude Code 的产品定位。

- Memory 说明它想让会话不是一次性的，而是能积累长期上下文。
- Voice 说明它想把输入方式从键盘扩展出去。
- Buddy 说明它并不只追求功能，也在尝试建立产品人格和陪伴感。
- Keybindings、Output Styles、Skills、Plugins 说明它在把 CLI 做成可定制环境。

也就是说，Claude Code 不是只想做“代码补全器”，而是在尝试成为一个长期驻留、可扩展、有状态、有界面的 AI 工作台。

---

## 11. Rust 重写说明了什么

`claude-code-rust/` 不是给 TypeScript 补几个高性能模块，而是一套完整独立重写。

它保留了同样的产品语义：

- 相同的工具名称和权限模型
- 相同的查询循环和 compact 思路
- 相同的 MCP、bridge、cron 等外围能力

但实现语言换成了 async Rust，按 crate 拆成 `core / api / tools / query / tui / commands / mcp / bridge / cli` 九块。

这说明团队对现有架构的理解已经足够稳定，稳定到可以把“产品语义”和“实现语言”分离开来。换句话说，Claude Code 的核心设计已经不依赖 TypeScript 本身，而是一套更高层的系统模型。

---

## 12. 我的整体理解

如果只用一句话总结：

Claude Code 是一个以查询循环为核心、以工具系统为执行面、以权限系统为边界、以自定义终端 UI 为交互壳、再通过 bridge/MCP/plugins/skills 向外扩展的 agent 平台。

它之所以复杂，不是因为文件多，而是因为它同时解决了四类问题：

- 模型如何持续推理
- 模型如何安全执行
- 用户如何稳定交互
- 系统如何跨端和扩展

这四件事少一件，Claude Code 都会退化成普通聊天 CLI、普通脚本 runner、普通 TUI，或者普通 IDE 插件。正是因为这四件事同时成立，它才变成今天这套完整系统。
